"use strict";

/*build config*/
var FileConfig = {
    /* 打包输出位置 */
    build: {
        tempPath: '.temp',
        rootPath: 'build'
    },
    /* src路径 */
    src: {
        rootPath: 'src'
    }
};

module.exports = FileConfig;
