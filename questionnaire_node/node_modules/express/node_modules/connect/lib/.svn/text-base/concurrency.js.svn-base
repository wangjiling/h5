exports.concurrencyHandler = function (req, res, out) {
    //console.log("call with listener " + this.name);
    var server = this;
    req.addListener('end', function () {
        server.handle(req, res, out);
    });
}